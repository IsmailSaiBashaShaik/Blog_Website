<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        body {
            background-color: #4584c3; /* Light gray background */
            color: #212529; /* Dark text color */
        }

        .container {
            margin-top: 50px;
        }

        h1 {
            color: #007bff; /* Blue heading color */
        }

        form {
            background-color: #f1ce84; /* White form background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Subtle shadow */
        }

        label {
            color: #007bff; /* Blue label color */
        }

        input {
            border: 1px solid #007bff; /* Blue border for input fields */
            border-radius: 5px;
            padding: 8px;
            margin-bottom: 10px;
        }

        button {
            background-color: #007bff; /* Blue button background */
            color: #ffffff; /* White text on button */
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
    </style>
    <title>Blog Website</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Edit Article</h1>
        <form action="/articles/<%=article.id %>?_method=PUT" method="POST">
            <%- include('form_field') %>
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
